<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ScreenTimeForSleepy" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- 主题色 -->
        <item name="colorPrimary">@color/theme_color</item>
        <item name="colorPrimaryDark">@color/theme_color</item>
        <item name="colorAccent">@color/theme_color</item>
        
        <!-- Switch 主题色 -->
        <item name="colorControlActivated">@color/theme_color</item>
        <item name="android:colorControlActivated">@color/theme_color</item>
    </style>
</resources>
